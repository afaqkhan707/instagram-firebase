import React from 'react';
import { StyleSheet, Text, View } from 'react-native';

const DisplayReels = () => {
  return (
    <View>
      <Text>Display Reels</Text>
    </View>
  );
};

export default DisplayReels;

const styles = StyleSheet.create({});
